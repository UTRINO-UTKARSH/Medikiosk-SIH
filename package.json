{
    "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "python": "python Backend/audio.py",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\" \"npm run python\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}